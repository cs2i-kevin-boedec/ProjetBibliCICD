# name: "OWASP Dependency-Check"

# on:
#   push: {}

# jobs:
#   dependency-check:
#     name: Run Dependency-Check on push
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Set up JDK 17
#         uses: actions/setup-java@v4
#         with:
#           distribution: 'temurin'
#           java-version: '17'

#       - name: Cache Maven local repository
#         uses: actions/cache@v4
#         with:
#           path: ~/.m2/repository
#           key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}

#       - name: Cache dependency-check DB
#         uses: actions/cache@v4
#         with:
#           path: ProjetBibliotheque/bibliotheque/target/dependency-check-data
#           key: ${{ runner.os }}-depcheck-${{ hashFiles('ProjetBibliotheque/bibliotheque/pom.xml') }}

#       - name: Run OWASP Dependency-Check
#         working-directory: ProjetBibliotheque/bibliotheque
#         run: mvn -B org.owasp:dependency-check-maven:check

#       - name: Upload dependency-check reports
#         uses: actions/upload-artifact@v4
#         with:
#           name: dependency-check-report
#           path: ProjetBibliotheque/bibliotheque/target/dependency-check-report.*
